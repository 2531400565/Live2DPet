# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)：MAJOR 为破坏性变更，MINOR 为向后兼容的新功能，PATCH 为问题修复。

## [1.4.0] - 专注设置化与配置版本化

一句话：**把番茄钟从"写死"变成"可配"，并给配置文件上了版本保险。**

### 新增

- **番茄钟设置化**（设置 → 专注页）
  - 专注时长 1–180 分、短休 1–60 分、提醒间隔 1–60 分，范围内钳位，非法值回退默认。
  - 每日专注目标 0–24 段（0 = 关）；达成时切换庆祝气泡。
  - 空闲改时长立即生效；进行中不打断，回到 `Idle` 自动按新设置重建会话。
- **专注成就延伸**：新增 `focus_25`「心无旁骛」（累计 25 次）/ `focus_100`「专注大师」（累计 100 次）。
- **养成统计页**新增「累计专注 / 今日专注」两行（今日按自然日跨天重置）。
- **设置版本化**：`settings.json` 增加 `_version` 字段，缺失 / 损坏 / 更高版本均按 v1 安全兼容，不阻断启动。

### 工程

- 单元测试 185 → 204（新增 `FocusDailyTests` 9 例、`FocusExtendedAchievementsTests` 4 例、`SettingsVersionTests` 6 例）。
- 自有 4 个项目 Debug / Release 保持 **0 Error / 0 Warning**（9 条警告均为 `thirdparty/Live2DCSharpSDK` 历史遗留，容忍）。

## [1.3.0] - 起名、台词自定义、番茄钟、透明截图、Welcome Back

一句话：**让桌宠更"像你的"，并可留下透明影像。**

### 新增

- **给宠物起名**：昵称设置，所有台词通过 `{name}` 占位统一替换。
- **台词自定义**：`config/dialogue.json` 覆盖 12 组台词，首次生成带注释模板，文件损坏自动回退默认；并引入 `_version: 1`，解析忽略所有 `_` 开头字段。
- **番茄钟陪伴**（`FocusSession` 纯状态机 `Idle / Focus / Break`，可单测）
  - 托盘动态菜单开始 / 结束专注；每 5 分钟气泡提醒。
  - 专注期间抑制环境气泡（免打扰）。
  - 完成奖励经验（常量 `FocusRewardExp = 10`）+ 终身累计 `TotalFocusSessions`。
  - 成就 `focus_1`「初试专注」/ `focus_10`「专注达人」。
- **透明 PNG 导出**：托盘一键保存仅桌宠透明图，默认存 `图片/Live2DPet/Pet_yyyy-MM-dd_HHmmss.png`，Alpha 反预乘（无额外依赖）。
- **Welcome Back**：记录启动 / 退出时刻（UTC），按离开时长播放首启 / 欢迎文案，昵称自然融入。

## [1.2.0] - 自动更新

一句话：**发布链路彻底闭环**——从此 GitHub 发版后，桌宠自己就能发现、下载、校验并热替换升级，用户不必再手动下 zip。

### 新增

- **自动更新**
  - 托盘菜单新增「检查更新…」；启动时可静默检查 GitHub Release（`CheckUpdateOnStartup` 开关，默认开）。
  - 发现新版本：启动检查仅弹托盘气泡提示，不自动下载；手动点击才进入下载流程。
  - 下载带进度条；完成后用 Release 资产的 **SHA256 校验**防损坏/篡改，校验失败拒绝应用。
  - 校验通过后写入升级脚本（robocopy 覆盖安装目录），由脚本等待本进程退出后复制并重启，无需额外 updater 程序。
  - 设置 → 提醒 页增加「启动时自动检查更新」开关。
  - 单元测试覆盖 Release JSON 解析、版本比较、SHA256 比对（11 例）。

## [1.1.0] - 稳定性与工程

一句话：**让桌宠在真实使用环境里更"耐造"**——高分屏不糊、睡醒了不跳、显卡抽风了能自己爬起来、数据丢了能找回来。

### 新增

- **高 DPI 自适应**
  - 监听 `WM_DPICHANGED`，拖到不同缩放的显示器或改系统缩放时，按新 DPI 重算渲染分辨率与画布尺寸。
  - 画布基准尺寸以 96 DPI 定义，200% 缩放下按 2 倍分辨率渲染，避免位图被系统拉伸导致的模糊与边缘发虚。
- **休眠 / 唤醒自恢复**
  - 进入休眠：立即暂停渲染并保存养成进度。
  - 唤醒后：重置渲染时间基准（不把几小时的休眠算进一帧，避免动画与状态跳变）、重置在线计时、把桌宠拉回可见区域。
  - 系统时间被大幅修改（手动改表 / 域同步 / 休眠校时）时同样重置在线计时基准。
- **渲染故障自恢复**
  - 渲染连续失败达阈值（GL 上下文丢失 / 驱动重置 / 独显切换）或持续 10 秒空帧时，自动保存状态、释放单实例锁并"接力重启"。
  - 带冷却时间与次数上限，不会陷入重启死循环。
- **配置备份 / 还原**
  - 设置、养成进度、参数映射打包成一个 zip（设置 → 养成页）。
  - 还原前二次确认，校验通过才落盘；白名单过滤 + 拒绝路径穿越，不会"还原个备份把程序搞坏"。
- **统一运行日志**
  - `logs/app.log`，按 256 KB 滚动，保留 3 份历史；启动、模型参数解析、崩溃、恢复全程留痕。
  - 托盘菜单新增「查看日志…」，一键打开日志目录。

### 改进

- 显示器配置变化（改分辨率 / 拔插外接屏 / 投影切换）后自动把桌宠拉回可见区域并保存新位置。
- 模型参数解析日志从控制台输出改为写入日志文件（WinExe 下原本无处可看）。

### 工程

- 新增 `Live2DPet.Core/AppLog.cs`（轻量滚动日志，各层共用，写入失败一律静默）。
- 新增 `Live2DPet.Core/Settings/ConfigBackup.cs`（备份 / 还原，含 7 组单元测试）。
- `Live2DPet.Platform` 增加对 `Live2DPet.Core` 的引用（日志共用）。
- 单元测试 49 → 57。

## [1.0.0] - 首个正式版

- 透明置顶分层窗口、Live2D 实时渲染、鼠标 / 键盘互动、表情动作联动。
- 养成系统（好感 / 等级 / 羁绊）、成就、签到与离线补偿、节日彩蛋。
- 免打扰、闲置打盹、贴边半隐藏、一键截图、音效音量、崩溃自启。
- 设置持久化与 `petstate` 备份容灾、多模型、开机自启、多屏位置记忆、单实例。
